# Copy to .env and adjust for your machine
EXPO_PUBLIC_API_URL=http://localhost:5001/api

# Optional: Mac LAN IP for Expo Go on a physical device (when auto-detect fails)
# EXPO_PUBLIC_DEV_LAN_HOST=192.168.1.12
